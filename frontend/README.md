# Frontend PT UNZANET

Frontend Next.js App Router + React + TypeScript.

```powershell
npm install
Copy-Item .env.example .env.local
npm run dev
```

Atur `NEXT_PUBLIC_API_URL` bila backend tidak berjalan di `http://localhost:3001/api`.
